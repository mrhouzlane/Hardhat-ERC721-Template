name : Github actions 

on: push

jobs:
  build:
    runs-on: macos-latest
      
    steps:
      - uses: actions/stale@v5
      - name: running tests
        run: |
          npx hardhat init 
          npx hardhat compile 
          npx hardhat test 
      
    
    
  
  
